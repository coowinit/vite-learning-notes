# screenshots

本目录用于保存 README 中引用的学习过程截图。

当前截图：

- `01-node-npm-version.png`：检查 Node.js 和 npm 版本。
- `02-vite-localhost-preview.png`：Vite 默认页面与 localhost 预览。
- `03-src-default-files.png`：Vite 默认 `src` 目录文件。
- `04-root-directory-files.png`：Vite 项目根目录文件。
